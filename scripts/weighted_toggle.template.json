{
  "description": "${DESCRIPTION}",
  "enable": true,
  "flippingStrategy": {
    "initParams": {
      "weight": "${WEIGHT}"
    },
    "type": "org.ff4j.strategy.PonderationStrategy"
  },
  "permissions": [
    "${PERMISSIONS}"
  ],
  "uid": "${WEIGHTED_TOGGLE_NAME}"
}

